<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ops.example.backend.mapper.UserMapper">
    <select id="selectAll" resultType="ops.example.backend.entity.User">
        SELECT * FROM user
        <where>
            <if test="username != null"> username like concat('%', #{username}, '%')</if>
            <if test="status != null">and status = #{status}</if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insert">
        insert into user (username, password, nickname, phone, email, status)
        values(#{username}, #{password}, #{nickname}, #{phone}, #{email}, #{status})
    </insert>

    <update id="updateById">
        update `user` set username = #{username}, nickname = #{nickname}, phone = #{phone}, email = #{email}, status = #{status}
        where id = #{id}
    </update>
</mapper>                             