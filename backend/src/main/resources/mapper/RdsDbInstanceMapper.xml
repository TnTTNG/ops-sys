<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ops.example.backend.aliyun.mapper.RdsDbInstanceMapper">
    
    <select id="selectAll" resultType="ops.example.backend.aliyun.entity.RdsDbInstance">
        SELECT * FROM rds_db_instance
    </select>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO rds_db_instance (
            db_instance_id, description, region_id, status,
            engine, engine_version, created_at, updated_at
        ) VALUES 
        <foreach collection="list" item="item" separator=",">
            (
                #{item.dbInstanceId}, #{item.description}, #{item.regionId},
                #{item.status}, #{item.engine}, #{item.engineVersion},
                #{item.createdAt}, #{item.updatedAt}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
            description = VALUES(description),
            region_id = VALUES(region_id),
            status = VALUES(status),
            engine = VALUES(engine),
            engine_version = VALUES(engine_version),
            created_at = VALUES(created_at),
            updated_at = VALUES(updated_at)
    </insert>
    
</mapper> 