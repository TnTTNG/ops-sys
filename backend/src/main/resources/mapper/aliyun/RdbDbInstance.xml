<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aliyun.mapper.RdsDbInstanceMapper">

    <select id="selectAll" resultType="com.aliyun.entity.RdsDbInstance">
        SELECT * FROM `rds_db_instance`
        <where>

        </where>
        ORDER BY id DESC
    </select>
</mapper>